
type ActionsType = {}
const initialState:any = [
]

export const loginReducer = (state = initialState, action:any):any => {
    switch (action.type) {

        default:
            return state;
    }
}

