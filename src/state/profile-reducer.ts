type ActionsType = {}
const initialState:any = [
]

export const ProfileReducer = (state = initialState, action:any):any => {
    switch (action.type) {

        default:
            return state;
    }
}