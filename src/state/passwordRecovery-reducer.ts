type ActionsType = {}
const initialState:any = [
]

export const PasswordRecoveryReducer = (state = initialState, action:any):any => {
    switch (action.type) {

        default:
            return state;
    }
}