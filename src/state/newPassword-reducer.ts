
type ActionsType = {}
const initialState:any = [
]

export const newPasswordReducer = (state = initialState, action:any):any => {
    switch (action.type) {

        default:
            return state;
    }
}

