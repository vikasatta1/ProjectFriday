
type ActionsType = {}
const initialState:any = [
]

export const RegistrationReducer = (state = initialState, action:any):any => {
    switch (action.type) {

        default:
            return state;
    }
}